# Secrets — there are none today, but .env.prod is where a deployment's
# settings live and it is never committed.
.env
.env.local
.env.*.local
.env.prod

# .NET
bin/
obj/
*.user
TestResults/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Editors / OS
.DS_Store
.idea/
.vscode/
